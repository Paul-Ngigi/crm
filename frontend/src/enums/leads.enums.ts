export enum LeadStatus {
  NEW = 'new',
  CONTACTED = 'contacted',
  QUALIFIED = 'qualified',
  CONVERTED = 'converted',
}

export enum LeadInteractionType {
  CALL = 'call',
  EMAIL = 'email',
  SOCIAL_MEDIA = 'social_media',
  MEETING = 'meeting',
}
